apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rideprice-ingress
spec:
  rules:
  - host: rideprice.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nginx-service
            port:
              number: 80
